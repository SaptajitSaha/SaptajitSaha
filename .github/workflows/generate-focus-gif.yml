name: Generate Focus Bars GIF

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/generate-focus-gif.yml'
      - 'assets/focus-bars.svg'

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate animated GIF
        run: |
          python - <<'PY'
          from PIL import Image, ImageDraw, ImageFilter
          import math
          from pathlib import Path

          W, H = 760, 190
          frames = 48
          out = Path('assets/focus-bars.gif')
          out.parent.mkdir(parents=True, exist_ok=True)

          labels = [('DATA SCIENCE', 295), ('AI / ML', 262), ('QUANT FINANCE', 197), ('OPEN SOURCE', 197)]
          y_positions = [23, 63, 103, 143]
          x0, track_w, bar_h = 190, 360, 18

          stops = [
              (0.00, (255, 0, 76)),
              (0.20, (255, 0, 230)),
              (0.42, (106, 0, 255)),
              (0.64, (0, 200, 255)),
              (0.82, (0, 255, 157)),
              (1.00, (255, 157, 0)),
          ]

          def interp(a, b, t):
              return tuple(int(a[i] + (b[i] - a[i]) * t) for i in range(3))

          def rainbow(t):
              t %= 1.0
              for i in range(len(stops) - 1):
                  p1, c1 = stops[i]
                  p2, c2 = stops[i + 1]
                  if p1 <= t <= p2:
                      return interp(c1, c2, (t - p1) / (p2 - p1))
              return stops[-1][1]

          def rounded_gradient(width, height, phase):
              img = Image.new('RGB', (width, height), (0, 0, 0))
              px = img.load()
              for x in range(width):
                  t = (x / max(1, width - 1) + phase) % 1.0
                  c = rainbow(t)
                  for y in range(height):
                      v = 0.82 + 0.18 * (1 - abs((y / max(1, height - 1)) - 0.5) * 2)
                      px[x, y] = tuple(int(v * q) for q in c)
              mask = Image.new('L', (width, height), 0)
              md = ImageDraw.Draw(mask)
              md.rounded_rectangle((0, 0, width - 1, height - 1), radius=6, fill=255)
              img.putalpha(mask)
              return img

          result = []
          for f in range(frames):
              base = Image.new('RGBA', (W, H), (13, 13, 13, 255))
              d = ImageDraw.Draw(base)
              d.rounded_rectangle((1, 1, W - 2, H - 2), radius=13, outline=(38, 38, 38, 255), width=1)

              for text, width in labels:
                  pass

              try:
                  font = __import__('PIL').ImageFont.truetype('/usr/share/fonts/truetype/dejavu/DejaVuSansMono-Bold.ttf', 15)
              except Exception:
                  font = None

              for idx, ((text, width), y) in enumerate(zip(labels, y_positions)):
                  d.text((24, y + 1), text, fill=(245, 245, 245, 255), font=font)
                  d.rounded_rectangle((x0, y, x0 + track_w, y + bar_h), radius=6, fill=(23, 23, 23, 255), outline=(43, 43, 43, 255))

                  phase = (f / frames) * (1.0 if idx % 2 == 0 else -1.0) + idx * 0.13
                  glow = Image.new('RGBA', (W, H), (0, 0, 0, 0))
                  grad = rounded_gradient(width, bar_h, phase)
                  glow.paste(grad, (x0, y), grad)
                  glow_blur = glow.filter(ImageFilter.GaussianBlur(6))
                  base.alpha_composite(glow_blur)
                  base.alpha_composite(glow)

                  highlight = Image.new('RGBA', (width, bar_h), (0, 0, 0, 0))
                  hd = ImageDraw.Draw(highlight)
                  hd.rounded_rectangle((2, 2, width - 3, 5), radius=2, fill=(255, 255, 255, 55))
                  base.alpha_composite(highlight, (x0, y))

              result.append(base.convert('P', palette=Image.Palette.ADAPTIVE))

          result[0].save(out, save_all=True, append_images=result[1:], duration=75, loop=0, optimize=False, disposal=2)
          PY

      - name: Commit GIF
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/focus-bars.gif
          git diff --cached --quiet || git commit -m "chore: refresh animated focus bars"
          git push
